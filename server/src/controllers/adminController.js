export async function listAdminProducts(req, res) {
  res.json({
    message: 'Admin products endpoint placeholder ready for CRUD implementation.',
  })
}

export async function listAdminOrders(req, res) {
  res.json({
    message: 'Admin orders endpoint placeholder ready for dashboard implementation.',
  })
}
